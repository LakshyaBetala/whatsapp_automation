# --- Backend (Railway) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
apscheduler==3.11.0
supabase==2.11.0
jinja2==3.1.5
python-dateutil==2.9.0.post0

# PDF generation. WeasyPrint needs native libs (GTK/Pango/Cairo).
# On Windows install the GTK3 runtime; on Railway the buildpack includes them.
weasyprint==63.1

# UPI QR codes attached to reminders
qrcode[pil]==8.0

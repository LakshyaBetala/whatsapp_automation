# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
# Secrets
.env
*.env
.env.*
!.env.example
.admin_key
# PyInstaller (agent build artifacts)
tally_agent/build/
tally_agent/dist/
*.spec
# WeasyPrint / generated
*.pdf
generated/
# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/
# Ignore large binary and output files
build/
dist/
TallyAgentRelease/
Asva/
*.xml
*.txt
!requirements.txt
# Scratch / debug scripts (root only - do not catch app/ package modules)
/batch*.py
/dump_*.py
/analyze_*.py
/parse_*.py

# Agent config holds the live agent_token — never commit
tally_agent/config.json
# Per-customer agent bundles (contain live tokens)
agents/

# Node
node_modules/

# Pilot artifacts
pilot_saas.db
agent.log

# managed
**.data.json
**.node-persist**
**_IGNORE_**
# end managed

# WhatsApp session data (Chromium profiles — machine-local, contains auth)
wa_service/.wwebjs_auth/
wa_service/.wwebjs_cache/
wa_service/.baileys_auth/

# Installer build artifacts (large binaries, rebuilt by build_installer.ps1)
dist_installer/
dist_agent/
build_agent/

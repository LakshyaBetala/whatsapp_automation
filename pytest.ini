[pytest]
# Unit tests only; live-Tally probes live outside the repo now.
# dev/ holds integration scripts that talk to the local Supabase (needs env);
# they are run by hand via dev/run_dev.sh, never in the unit suite.
norecursedirs = dev .venv .git dist_installer dist_agent build_agent Asva
